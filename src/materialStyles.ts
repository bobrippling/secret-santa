export default (theme:any) => ({
    paperNoPadding: {
        margin: theme.spacing(3)
    },
    paperTinyWidth: {
        width: '30%',
        marginLeft: 'auto',
        marginRight: 'auto'
    }
});
